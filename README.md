The objective is to create a system that generates a 6-digit OTP, sends it to the user's email, and then verifies the OTP when the user enters it. If the OTP matches, the user gets access; if not, access is denied.
